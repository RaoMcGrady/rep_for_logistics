一、记录一些控件
（1）名称：jquery省市区城市地图选择器插件【原创】
（2）网站：https://www.sucaihuo.com/js/2102.html
二、控件radio使用jQuery控制选择。
  <1>、$("input:radio[name='carrierinfo_settle_ismenu'][value=1]").click();
  使用attr("checked",'checked');prop('checked',true); 不管用；

三、多联物流功能模块-通信协议
1、善驾的记录（mobile）

(1)、添加一个控制器，并且需要做标识（@Controller）。
(2)、控制器中添加方法[@RequestMapping(value="userReg.do",method= RequestMethod.POST)、 @ResponseBody]
(3)、@responsebody表示该方法的返回结果直接写入HTTP response body中
(4)、返回值是ImmutableMap<K, V>他是继承与Map<K, V>。
(5)、spring-mvc.xml文件  <!--静态资源跳过springMvc的dispatcher servlet的拦截--> 这个要进行是否拦截配置。

  
